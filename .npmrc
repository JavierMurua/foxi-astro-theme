# Enforce pnpm architecture
shamefully-hoist=false

# Security: Prevent automatic execution of potentially malicious scripts in dependencies
# Note: You may need to run 'pnpm install' with '--ignore-scripts=false' for specific trusted packages if needed
ignore-scripts=true

# Ensure strict lockfile usage in CI/production
frozen-lockfile=true
